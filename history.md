# 여행 트레커 작업 이력

브라우저에서 사진 GPS로 여행 동선을 재생·저장하는 단일 페이지 앱 (`index.html`) 개발 기록.

---

## 이번 세션 (2026-08-21) — DOM 그대로 동영상 저장

### 문제
- 녹화 중 화면(DOM UI)과 다운로드 MP4 내용이 달랐음.
- 원인: 화면은 MapLibre HTML 오버레이(핀·카드)인데, 저장은 `drawExportFrame`으로 **오프스크린 캔버스에 다시 그려** MediaRecorder에 넣던 이중 경로.

### 결정·구현
- 캔버스 재합성 제거 → **이 탭 공유** (`getDisplayMedia` + `preferCurrentTab`) + `#mapwrap` 영역 캡처 (`RestrictionTarget` / `CropTarget`).
- 녹화 중 조작 UI만 `#mapwrap.is-recording`으로 `display:none` (타임라인·햄버거·핏·상세·맵 컨트롤).
- 지도·핀·스팟 카드·Day 배너·상단 진행·인트로·종료 요약은 DOM 그대로 영상에 포함.
- 진행 배지 `#recordStatus`는 mapwrap 밖 (영상에 안 들어가게).
- 카톡 등 OS 알림은 탭/영역 캡처라 포함되지 않음. 저장 시마다 «이 탭» 공유 허용 필요.

### 관련 커밋
| 해시 | 메시지 |
|------|--------|
| `3f0aa0a` | 돔영 그대로 저장 |
| `ca863a3` | 최대구간속도 늘리기 |

---

## 재생·카메라·도착 UI

| 커밋 | 요약 |
|------|------|
| `7b8fb72` | 재생 중 여행 제목 인트로 카드 + 상단 진행 바 |
| `8537104` | 짧은 구간(타이트 홉)에서 잠깐 줌인해 핀·경로 보이게 |
| `48018b6` | 도착 UI를 컴팩트 스팟 카드로 통일, 재생 UX 정리 |
| `ed7a8ba` | 시네마틱 카메라 리듬 (미리보기·MP4 공통) |
| `2722b8e` | 여행자 추종 복구, 짧은 줌 부스트, 녹화 전용 오버뷰 |
| `b55989b` | 홉 카메라가 `cameraForBounds`를 과도하게 넘지 않도록 |
| `2cc9ef9` | 포토 팬 → 이동만 / 팝업 도착 연출 모드 |
| `d93d6c6` | bounds 중심 카메라, 포토 팬 옵션, 오버뷰 영상 인트로 |
| `8d4685c` | 구간마다 A→B fit 줌으로 시작 (전체 여행 오버뷰 대신) |
| `62097c1` | 도착 시 장소명 버블, dwell 중 다음 홉 리프레임 |
| `5a518c0` | 홉별 A→B fit 줌 추종 |
| `766f0ac` | 롱홉 스무딩 후 미리보기가 너무 줌아웃되던 문제 수정 |
| `6d9ce02` | 세그먼트별 카메라 타깃 고정으로 줌 안정화 |
| `95c6aad` | 경로 재생 줌 스무딩, 웨이포인트를 첫 GPS 사진에 고정 |
| `bcd6f4b` | 재생 중 사진 핀이 경로보다 늦던 문제 (Marker transform 규칙) |

---

## 동영상 저장·스타일·경로

| 커밋 | 요약 |
|------|------|
| `36f2a14` | 전체 경로 / 지나간 경로 색 분리, 미리보기 vs 저장, MP4 강제 |
| `59ff398` | 영상보내기, Lucide 이동 아이콘, 거리 기반 재생 카메라 |
| `7ad9127` | 베이스맵 테마 전환 후 경로선 복구 |
| `4bd5273` | 방문 순 경로선·카메라 스로틀 관련 되돌림 |
| `666c343` | 경로선이 고유 장소가 아니라 실제 방문 시간순을 따르게 |

---

## 클러스터·사이드바·모바일

| 커밋 | 요약 |
|------|------|
| `de163cc` | 제목 변경 |
| `ee17fed` | 사이드바 기본값, 삭제 액션, 재생 카메라 폴리시 |
| `66f5c84` | 줌 적응형 클러스터 → 하루/도시/국가 스케일 프리셋 |
| `e5421d3` | 줌 재클러스터 후 장소명 미해결 수정 |
| `f7a6279` | 사이드바를 한 덩어리로 스크롤, 초기 fit이 사이드바와 겹치지 않게 |
| `723cac9` | 초기 클러스터가 fit 전 줌을 쓰던 버그 |
| `ebe04df` | 핀치 줌과 줌 적응 재클러스터가 싸우던 문제 |
| `be43417` | iPhone Chrome에서 floating 버튼이 가려지던 safe-area 수정 |

---

## 현재 아키텍처 요약

```
사진 폴더/파일 (로컬 only)
  → EXIF GPS/시각 → 클러스터
  → MapLibre 지도 + HTML 핀/카드
  → 미리보기: requestAnimationFrame + tickPlayback
  → 저장: 탭 공유로 #mapwrap DOM 캡처 → MediaRecorder → (필요 시) FFmpeg.wasm MP4
```

- **단일 소스 UI:** 화면 DOM = 저장 영상 (조작 chrome만 녹화 시 숨김).
- **계획 문서:** `JOURNAL_UI_PLAN.md` (저널 오버레이 시안 — 별도 진행).

---

## 수동 확인 체크 (DOM 저장)

- [ ] 저장 → «이 탭» 허용 → 영상에 핀·카드·배너가 화면과 동일
- [ ] 타임라인·햄버거·핏·맵 컨트롤·녹화 상태 배지가 영상에 없음
- [ ] 저장 중 카톡 알림이 영상에 안 남음
- [ ] 공유 거부 시 앱이 깨지지 않고 안내만
